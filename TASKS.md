# Tasks

Queue of work for Claude. Add new tasks to the bottom. Do not remove or edit an
unchecked task unless you are starting it.

## Rules
- Work on only one task at a time.
- New tasks go at the bottom of the list.
- Do not abandon or interrupt the current task unless the user explicitly says "interrupt".
- Finish, test, and verify the current task before starting the next.
- Before starting another task, re-read this file and select the oldest pending (unchecked) task.
- After completing a task, check it off, briefly tell the user it's done, and state which task is starting next.
- Do not combine unrelated tasks into one implementation.
- When you check a task off, indent a short `Done YYYY-MM-DD:` note under it saying what
  actually changed. If the task cannot be finished, leave it unchecked and add a
  `Blocked YYYY-MM-DD:` note explaining what is needed to unblock it.

## Queue
- [x] Keep users signed in for 30 days instead of getting logged out (extend session lifetime from 7 to 30 days).
  - Done 2026-09-19: Bumped `SESSION_DAYS` in `web/src/lib/auth.ts` from 7 to 30. Both the session-store expiry and the `ifr_session` cookie `maxAge` derive from this single constant, so both now last 30 days. Verified with `npm run build`.
- [x] Remember video playback position per lesson so resuming a video continues where the user left off instead of restarting from 0:00.
  - Done 2026-09-19: Added a `playback_position` table (SQLite) / `playback/{userId}` blob key (Netlify), `getPlaybackPositions`/`setPlaybackPosition` in `web/src/lib/data.ts`, and a new `web/src/pages/api/playback-position.ts` GET/POST endpoint. The course player script (`web/src/pages/course/[courseId].astro`) now keys each video by `lessonId/tabId/fileIndex`, seeks to the saved position on load (skipping the last few seconds), and saves progress every 5s during playback plus on pause/ended/tab-hide/page-hide. Verified end-to-end with the dev server: setup/login, POST/GET round-trip and update on `/api/playback-position`, 400 on missing fields, 401 when unauthenticated, and confirmed the session cookie now expires in 30 days.
- [x] Add a "Sing the Numbers / Feel the Numbers" ear-training module that plays all of those audio tracks in order, back to back, with autoplay advancing to the next track.
  - Done 2026-09-19: Asked the user to clarify "Feel the Numbers" since no such label exists anywhere in the course data (closest candidates were the "Meditative"/"Advanced" tracks) — they said to skip it for now. Built a new "Sing the Numbers" course (`web/src/lib/courses.ts`, id `sing-the-numbers`) with one lesson per chord (1, 2-, 4, 5D, 6-, 7-b5) for each track explicitly labeled "Sing the Numbers" in the existing Seven Worlds data, in chord-progression order. Added an `autoAdvance` flag on `Course`; the course page (`web/src/pages/course/[courseId].astro`) now autoplays each track and, on `ended`, chains to the next file in the tab or calls a new `goToNextLesson()` (extracted from the existing next-lesson button) to advance lessons automatically — scoped only to courses with `autoAdvance: true` so no other course's behavior changed. Verified end-to-end against the dev server: course page renders all 6 lessons, and `/api/media/sing-the-numbers/<lesson>/main/0` resolves to the correct object key for all six (confirmed via "File Not Found" vs. a bogus lesson's "Not Found"). Note for the user: the 3- chord's "Ear Training Audio" track and the Harmonic Environment "Meditative"/"Advanced" tracks share the same underlying `ifr_sing_the_numbers_3_track_NN` numbering as the Sing the Numbers tracks, so they may be what "Feel the Numbers" refers to — worth a follow-up once that's confirmed.
- [x] Investigate and fix mobile users still being asked to log in every time despite the 30-day session.
  - Done 2026-09-19: Root cause is almost certainly the session cookie's `sameSite: 'strict'` setting in `web/src/lib/auth.ts`. Strict cookies are dropped by mobile browsers on top-level navigations that originate outside the page's own session — opening the site from a home-screen icon, a link tapped in another app, or a tab the OS evicted and reloaded — so those look like fresh, logged-out requests to the server. Changed it to `sameSite: 'lax'`, which still blocks the cookie on cross-site POSTs/embeds (this app has none to protect against) but is sent on ordinary top-level navigations. Verified with the dev server that the login response now sets `SameSite=Lax; Max-Age=2592000`. Note: if this doesn't fully resolve it, the next things to check are whether the phone's browser is in a private/incognito mode (cookies never persist there) or whether the phone is reaching the site via more than one hostname (a cookie set on one host isn't sent to another) — both would need info from the user's device/DNS setup that isn't visible from the code.
- [x] Default each lesson to reopen on whichever tab (Main/Guitar/Piano/Ear Training/Audio) the user was last on, instead of always defaulting to Main.
  - Done 2026-09-19: Added a `tab_id` column to `user_activity` (SQLite migration + Netlify Blobs record) and a new `setLastTab`/`web/src/pages/api/last-tab.ts` that records the tab whenever the user clicks one, without disturbing it on unrelated `setLastActivity` calls (e.g. the auto-complete-at-90% progress save). The course page now seeds the first lesson's tab from that saved value (falling back to the first tab if the remembered one doesn't exist on that lesson) and applies the same "last tab, else first tab" rule every time a lesson is selected client-side (sidebar click, mobile menu, prev/next buttons), so switching lessons keeps you on Guitar/Piano/etc. instead of resetting to Main. Verified end-to-end with the dev server: saving `guitar` for a Core Skills lesson and reloading the course page correctly seeded `initialTabId = "guitar"`; saving `guitar` for a Chord Melody lesson that only has a Main tab correctly fell back to `initialTabId = null`.

- [x] Replace Sing the Numbers sub-lessons with one autoplay playlist containing all audio from Documents/IFR, grouped by folder.
  - Done 2026-09-20: Unblocked without needing write access to R2. The user uploaded the four playlist folders to the bucket root via the Cloudflare dashboard, but `courses.ts` prefixed every key with `07. Sing the Numbers/`, so all 88 tracks would have 404'd. Rather than moving objects in R2 (which needs write permission the API token lacks — S3/R2 has no rename, only copy+delete), pointed the project at the real layout: `folder: ''` on the sing-the-numbers course, since each file path already begins with its playlist folder name. Updated `.gitignore` (so the four root folders can't be committed), the upload script's folder allowlist, and the README to match. Verified against the live bucket that all 88 referenced keys exist — 0 missing, 0 orphaned — and that all four tabs resolve their object keys while other courses are unaffected. Removed the obsolete move script.
  - Blocked 2026-09-20: Implemented a single master player and 88-track playlist grouped by all four source folders, with next/previous controls and autoplay across folders. Copied and checksum-verified all MP3s locally; production build and playback transition checks pass. R2 upload returns AccessDenied (403); credentials with object write access are needed to publish the audio. Added targeted folder upload support and matched the app’s path-style R2 addressing.
  - Blocked 2026-09-20 (this session): Checked this remote environment for a way to finish the upload myself — no `web/.env`/R2 credentials and no `07. Sing the Numbers/` audio folder exist here (this container is separate from whatever machine had local file access). Can't run the upload from here. Told the user the exact command already documented in README.md and that a 403 on the previous attempt means the R2 API token needs Object Read & Write permission on the bucket, not just read.

- [x] Allow local review without signing in.
  - Done 2026-09-20: Development requests on loopback use a dedicated local SQLite preview user; production authentication remains required. Disabled Netlify emulation for the local dev server. Verified the playlist returns HTTP 200 without cookies and the production build passes.

- [x] Show the four playlist names in the normal lesson-style sidebar.
  - Done 2026-09-20: Added matching sidebar navigation with four playlist names and track counts, active selection, and a responsive mobile layout. Selecting a playlist starts its first track; autoplay continues across playlists and updates the sidebar and visible track list.

- [x] Put Feel the Numbers fourth in the playlist order.
  - Done 2026-09-20: Reordered the master playlist to Sing the Numbers 1, 2, 3, then Feel the Numbers. The sidebar and autoplay sequence both follow this order.

- [x] Use a microphone icon for Sing the Numbers on the course index.
  - Done 2026-09-20: Added a microphone icon to the Sing the Numbers dashboard card.
- [x] Fix the Sing the Numbers playlist page's mobile layout so it matches other course pages: the sidebar's four playlists should collapse behind a hamburger menu instead of always being visible above the content.
  - Done 2026-09-20: `web/src/pages/course/sing-the-numbers.astro` now hides the desktop sidebar below `lg:` and adds a top bar with an "All Courses" link, a hamburger button, and a mobile title bar, plus a slide-over menu (backdrop, close button, Escape-to-close) containing the same four playlist buttons, mirroring `[courseId].astro`'s pattern. Selecting a playlist from the mobile menu closes it. Also fixed a bug the duplication would have caused: the "Playlist N of 4" label was computed from the DOM button's index, which would have read "of 8" once the buttons existed twice (desktop + mobile) and shown the wrong position depending on click order; it now derives the position from the group order in the already-available `tracks` data instead. Verified with the dev server (which now needs no login or netlify.toml workaround, per the teammate's other changes): confirmed the sidebar renders `hidden ... lg:flex`, all four mobile-menu elements are present, and the updated script passes a syntax check.
- [x] Keep the Sing the Numbers master player always visible while scrolling on mobile instead of it sitting under the nav bar.
  - Done 2026-09-20: The nav bar and the "Master player" card were two independent `sticky top-0` siblings, so both pinned to the same spot and the nav bar's higher z-index painted over the player's title text, leaving only the audio scrubber and buttons peeking out below it. Reproduced with a headless mobile-viewport browser and confirmed via `getBoundingClientRect()` that both rects were stuck at the same `top: 0`. Fixed by nesting both inside one shared `sticky top-0` wrapper so they stack in normal flow as a single cluster; verified the same way that after scrolling the player (`top: 81`) now sits cleanly below the nav bar (`bottom: 65`) with no overlap, confirmed visually with before/after screenshots.
- [x] Sing the Numbers should remember the last played track, not just the last course visited.
  - Done 2026-09-23: Reused the existing `setLastTab`/`getLastActivity` mechanism (built earlier for remembering a lesson's last-viewed tab) rather than adding new schema - the generic `tabId` slot now holds `groupId/fileIndex` for this playlist page, since it has no per-lesson tabs of its own to conflict with. The client posts to `/api/last-tab` every time a track is selected (row click, playlist switch, prev/next, or auto-advance on `ended`). Server-side, `sing-the-numbers.astro` looks up the saved value and renders the whole page around it from the start (title/group text, audio `src`, track-count, prev/next disabled state, highlighted row, visible section, active playlist tab in both the desktop sidebar and mobile menu) rather than always rendering track 1 and correcting client-side. Falls back to track 1 if there's no saved value or it doesn't match any track (e.g. a stale tab id left over from a regular course). Verified end-to-end against the dev server: saving `feel-the-numbers/5` correctly resumed at "Track 05" / "Feel the Numbers" / "70 of 88" with the right row, section, and playlist tab all marked current and neither nav button disabled; saving a non-matching value (`guitar`) cleanly fell back to track 1.
